# Принятые риски. Все без доступного фикса (affected/will_not_fix)
# и не достижимы в приложении. Пересмотреть при появлении фикса или смене функционала.

# cpython tarfile: приложение не распаковывает недоверенные tar
CVE-2025-13462
# expat XML DoS: приложение не парсит XML
CVE-2026-7210
# sqlite integer overflow: приложение не использует sqlite
CVE-2025-7458
# zlib MiniZip: Debian will_not_fix (код не собирается), приложение не использует
CVE-2023-45853